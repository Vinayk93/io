/*jslint node: true */
/*jshint globalstrict: true*/
'use strict';

var Requires = function (require) {
    this.require = require;
};

/* 
Require all the modules even before server start which check all codes 
present if not then make changes accordingly like file create and custom validation require
and middleware list

*/